'use strict';

class StateObserver extends StateModule {
  
  constructor(store) {
    super(store);
    
    // subscribe properties
    // store.subscribe
    
    this.properties = {
      a: 1
    };
  }
  
  
  
}
