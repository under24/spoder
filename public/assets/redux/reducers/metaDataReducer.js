'use strict';

let metaDataInitState = {
  coxaLength: 100,
  femurLength: 100,
  tibiaLength: 140,
};

const metaDataReducer = (state = metaDataInitState, action) => {
  switch (action.type) {
    case "a":
    
  }
  return state;
}