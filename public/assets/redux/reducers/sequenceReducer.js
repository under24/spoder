'use strict';

let sequenceReducerInitState = null;

const sequenceReducer = (state = sequenceReducerInitState, action) => {
  switch (action.type) {
    case "a":
      // return Object.assign({}, state, action.payload);
  }
  return state;
}
