'use strict';

let rotationInitState = {
  rotationModifier: 0,
  appliedTo: 'sagittalBaseY'
}

const rotationReducer = (state = rotationInitState, action) => {
  switch (action.type) {
    case "a":
      
  }
  return state;
}
