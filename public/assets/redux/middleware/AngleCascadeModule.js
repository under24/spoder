'use strict';

class AngleCascadeModule extends LogicReducer {
	
	constructor(store) {
		super(store);
		
		this.properties = {
			coords: 'coords'
		};
	}
	
	input(cascade) {
		switch (cascade) {
			case expression:
				
				break;
			default:
				
		}
	}
  
}